#!/usr/bin/env python3
"""
Logos CLI launcher.

This wrapper should behave like the installed `logos` command, including
subcommands such as `gateway`, `cron`, and `doctor`.

The legacy `hermes` wrapper alongside this file remains as a compatibility
alias (mirrors the deprecated `hermes` console-script alias in pyproject).
"""

if __name__ == "__main__":
    from logos_cli.main import main
    main()
